# Lite API only — copy to: .env (repository root, next to this file)
PORT=4000
JWT_SECRET=replace-with-strong-secret

# For the main Knex/Swagger app, use env.platform.example → finance-dashboard-platform/.env
